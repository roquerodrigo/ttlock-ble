name: CodeQL

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  schedule:
    - cron: "0 0 * * 0"

permissions:
  contents: read

jobs:
  codeql:
    permissions:
      actions: read
      contents: read
      security-events: write
    uses: roquerodrigo/.github/.github/workflows/sdk-codeql.yml@v2
